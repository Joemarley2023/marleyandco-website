media:
  input: assets
  output: /assets

content:
  - name: settings
    label: Site text
    type: file
    path: content/settings.json
    fields:
      - { name: heroHeadline, label: Hero headline, type: string }
      - { name: heroSub, label: Hero sub-line, type: text }
      - { name: email, label: Contact email, type: string }
      - { name: locationLine, label: Location line, type: string }
      - { name: instagram, label: Instagram URL, type: string }

  - name: projects
    label: Projects
    type: file
    path: content/projects.json
    fields:
      - name: projects
        label: Projects
        type: object
        list: true
        fields:
          - { name: title, label: Project name, type: string }
          - { name: location, label: Location (suburb, city), type: string }
          - { name: year, label: Year, type: string, required: false }
          - { name: slug, label: ID (lowercase, no spaces), type: string }
          - { name: description, label: Short description, type: text }
          - { name: materials, label: Key materials (comma separated), type: string, required: false }
          - { name: architect, label: Architect, type: string, required: false }
          - { name: interiorDesigner, label: Interior designer, type: string, required: false }
          - { name: builder, label: Builder, type: string, required: false }
          - { name: photographer, label: Photographer, type: string, required: false }
          - { name: instagramHook, label: Instagram hook (optional one-liner), type: text, required: false }
          - { name: lead, label: Lead image, type: image }
          - { name: secondary, label: Two detail images, type: image, list: true }
          - { name: gallery, label: Gallery photos, type: image, list: true }

  - name: testimonials
    label: Testimonials
    type: file
    path: content/testimonials.json
    fields:
      - name: testimonials
        label: Testimonials
        type: object
        list: true
        fields:
          - { name: quote, label: Quote, type: text }
          - { name: name, label: Name, type: string }
          - { name: practice, label: Practice / studio, type: string }
          - { name: placeholder, label: Is a placeholder?, type: boolean }

  - name: collaborators
    label: Collaborators
    type: file
    path: content/collaborators.json
    fields:
      - name: collaborators
        label: Collaborators
        type: object
        list: true
        fields:
          - { name: name, label: Name, type: string }
          - name: category
            label: Category
            type: select
            options:
              values:
                - architect
                - designer
                - builder
          - { name: logo, label: Logo (optional), type: image }
          - { name: placeholder, label: Is a placeholder?, type: boolean }
